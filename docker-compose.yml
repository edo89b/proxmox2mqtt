services:
  proxmox2mqtt:
    build: .
    container_name: proxmox2mqtt
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    env_file: .env
    networks:
      - mqtt_net

networks:
  mqtt_net:
    external: true
